<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.gbelib.app.intro.bookDescription.BookDescriptionDao">
	<select id="getDescription" resultType="String">
		SELECT
			DESCRIPTION
		FROM
			DESCRIPTION_INFO_TBL
		WHERE
			isbn = #{isbn}
		LIMIT 1
	</select>

	<insert id="addDescription">
		INSERT INTO DESCRIPTION_INFO_TBL
		(
			ISBN, DESCRIPTION
		) VALUES (
			#{0}, #{1}
		)
	</insert>
</mapper>
